/**
 * This code was AUTOGENERATED using the kinobi library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun kinobi to update it.
 *
 * @see https://github.com/metaplex-foundation/kinobi
 */

import {
  Context,
  Pda,
  PublicKey,
  Signer,
  TransactionBuilder,
  transactionBuilder,
} from '@metaplex-foundation/umi';
import {
  Serializer,
  array,
  mapSerializer,
  struct,
  u64,
  u8,
} from '@metaplex-foundation/umi/serializers';
import {
  ResolvedAccount,
  ResolvedAccountsWithIndices,
  getAccountMetasAndSigners,
} from '../shared';
import {
  UserPermissions,
  UserPermissionsArgs,
  getUserPermissionsSerializer,
} from '../types';

// Accounts.
export type ChangeDefaultPermissionsInstructionAccounts = {
  group: PublicKey | Pda;
  signer: Signer;
  systemProgram?: PublicKey | Pda;
};

// Data.
export type ChangeDefaultPermissionsInstructionData = {
  discriminator: Array<number>;
  groupId: bigint;
  newPermissions: UserPermissions;
};

export type ChangeDefaultPermissionsInstructionDataArgs = {
  groupId: number | bigint;
  newPermissions: UserPermissionsArgs;
};

export function getChangeDefaultPermissionsInstructionDataSerializer(): Serializer<
  ChangeDefaultPermissionsInstructionDataArgs,
  ChangeDefaultPermissionsInstructionData
> {
  return mapSerializer<
    ChangeDefaultPermissionsInstructionDataArgs,
    any,
    ChangeDefaultPermissionsInstructionData
  >(
    struct<ChangeDefaultPermissionsInstructionData>(
      [
        ['discriminator', array(u8(), { size: 8 })],
        ['groupId', u64()],
        ['newPermissions', getUserPermissionsSerializer()],
      ],
      { description: 'ChangeDefaultPermissionsInstructionData' }
    ),
    (value) => ({
      ...value,
      discriminator: [49, 126, 166, 232, 223, 128, 116, 81],
    })
  ) as Serializer<
    ChangeDefaultPermissionsInstructionDataArgs,
    ChangeDefaultPermissionsInstructionData
  >;
}

// Args.
export type ChangeDefaultPermissionsInstructionArgs =
  ChangeDefaultPermissionsInstructionDataArgs;

// Instruction.
export function changeDefaultPermissions(
  context: Pick<Context, 'programs'>,
  input: ChangeDefaultPermissionsInstructionAccounts &
    ChangeDefaultPermissionsInstructionArgs
): TransactionBuilder {
  // Program ID.
  const programId = context.programs.getPublicKey(
    'solchat',
    '9dBYNp2M6waWyDhpXptBAALAEuV5MHCZUUB9eTpufbbS'
  );

  // Accounts.
  const resolvedAccounts = {
    group: {
      index: 0,
      isWritable: true as boolean,
      value: input.group ?? null,
    },
    signer: {
      index: 1,
      isWritable: true as boolean,
      value: input.signer ?? null,
    },
    systemProgram: {
      index: 2,
      isWritable: false as boolean,
      value: input.systemProgram ?? null,
    },
  } satisfies ResolvedAccountsWithIndices;

  // Arguments.
  const resolvedArgs: ChangeDefaultPermissionsInstructionArgs = { ...input };

  // Default values.
  if (!resolvedAccounts.systemProgram.value) {
    resolvedAccounts.systemProgram.value = context.programs.getPublicKey(
      'splSystem',
      '11111111111111111111111111111111'
    );
    resolvedAccounts.systemProgram.isWritable = false;
  }

  // Accounts in order.
  const orderedAccounts: ResolvedAccount[] = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);

  // Keys and Signers.
  const [keys, signers] = getAccountMetasAndSigners(
    orderedAccounts,
    'programId',
    programId
  );

  // Data.
  const data = getChangeDefaultPermissionsInstructionDataSerializer().serialize(
    resolvedArgs as ChangeDefaultPermissionsInstructionDataArgs
  );

  // Bytes Created On Chain.
  const bytesCreatedOnChain = 0;

  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain },
  ]);
}
