/**
 * This code was AUTOGENERATED using the kinobi library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun kinobi to update it.
 *
 * @see https://github.com/metaplex-foundation/kinobi
 */

import {
  Context,
  Pda,
  PublicKey,
  Signer,
  TransactionBuilder,
  transactionBuilder,
} from '@metaplex-foundation/umi';
import {
  Serializer,
  array,
  mapSerializer,
  string,
  struct,
  u8,
} from '@metaplex-foundation/umi/serializers';
import {
  ResolvedAccount,
  ResolvedAccountsWithIndices,
  getAccountMetasAndSigners,
} from '../shared';

// Accounts.
export type InitializeUserInstructionAccounts = {
  user: PublicKey | Pda;
  owner: Signer;
  systemProgram?: PublicKey | Pda;
};

// Data.
export type InitializeUserInstructionData = {
  discriminator: Array<number>;
  rsaPublicKey: Array<number>;
  username: string;
};

export type InitializeUserInstructionDataArgs = {
  rsaPublicKey: Array<number>;
  username: string;
};

export function getInitializeUserInstructionDataSerializer(): Serializer<
  InitializeUserInstructionDataArgs,
  InitializeUserInstructionData
> {
  return mapSerializer<
    InitializeUserInstructionDataArgs,
    any,
    InitializeUserInstructionData
  >(
    struct<InitializeUserInstructionData>(
      [
        ['discriminator', array(u8(), { size: 8 })],
        ['rsaPublicKey', array(u8(), { size: 216 })],
        ['username', string()],
      ],
      { description: 'InitializeUserInstructionData' }
    ),
    (value) => ({
      ...value,
      discriminator: [111, 17, 185, 250, 60, 122, 38, 254],
    })
  ) as Serializer<
    InitializeUserInstructionDataArgs,
    InitializeUserInstructionData
  >;
}

// Args.
export type InitializeUserInstructionArgs = InitializeUserInstructionDataArgs;

// Instruction.
export function initializeUser(
  context: Pick<Context, 'programs'>,
  input: InitializeUserInstructionAccounts & InitializeUserInstructionArgs
): TransactionBuilder {
  // Program ID.
  const programId = context.programs.getPublicKey(
    'solchat',
    '9dBYNp2M6waWyDhpXptBAALAEuV5MHCZUUB9eTpufbbS'
  );

  // Accounts.
  const resolvedAccounts = {
    user: { index: 0, isWritable: true as boolean, value: input.user ?? null },
    owner: {
      index: 1,
      isWritable: true as boolean,
      value: input.owner ?? null,
    },
    systemProgram: {
      index: 2,
      isWritable: false as boolean,
      value: input.systemProgram ?? null,
    },
  } satisfies ResolvedAccountsWithIndices;

  // Arguments.
  const resolvedArgs: InitializeUserInstructionArgs = { ...input };

  // Default values.
  if (!resolvedAccounts.systemProgram.value) {
    resolvedAccounts.systemProgram.value = context.programs.getPublicKey(
      'splSystem',
      '11111111111111111111111111111111'
    );
    resolvedAccounts.systemProgram.isWritable = false;
  }

  // Accounts in order.
  const orderedAccounts: ResolvedAccount[] = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);

  // Keys and Signers.
  const [keys, signers] = getAccountMetasAndSigners(
    orderedAccounts,
    'programId',
    programId
  );

  // Data.
  const data = getInitializeUserInstructionDataSerializer().serialize(
    resolvedArgs as InitializeUserInstructionDataArgs
  );

  // Bytes Created On Chain.
  const bytesCreatedOnChain = 0;

  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain },
  ]);
}
