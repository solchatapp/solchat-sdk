/**
 * This code was AUTOGENERATED using the kinobi library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun kinobi to update it.
 *
 * @see https://github.com/metaplex-foundation/kinobi
 */

import {
  Context,
  Pda,
  PublicKey,
  Signer,
  TransactionBuilder,
  transactionBuilder,
} from '@metaplex-foundation/umi';
import {
  Serializer,
  array,
  mapSerializer,
  struct,
  u64,
  u8,
} from '@metaplex-foundation/umi/serializers';
import {
  ResolvedAccount,
  ResolvedAccountsWithIndices,
  getAccountMetasAndSigners,
} from '../shared';

// Accounts.
export type DeleteMessageInstructionAccounts = {
  group: PublicKey | Pda;
  signer: Signer;
  systemProgram?: PublicKey | Pda;
};

// Data.
export type DeleteMessageInstructionData = {
  discriminator: Array<number>;
  groupId: bigint;
  messageId: bigint;
};

export type DeleteMessageInstructionDataArgs = {
  groupId: number | bigint;
  messageId: number | bigint;
};

export function getDeleteMessageInstructionDataSerializer(): Serializer<
  DeleteMessageInstructionDataArgs,
  DeleteMessageInstructionData
> {
  return mapSerializer<
    DeleteMessageInstructionDataArgs,
    any,
    DeleteMessageInstructionData
  >(
    struct<DeleteMessageInstructionData>(
      [
        ['discriminator', array(u8(), { size: 8 })],
        ['groupId', u64()],
        ['messageId', u64()],
      ],
      { description: 'DeleteMessageInstructionData' }
    ),
    (value) => ({
      ...value,
      discriminator: [198, 99, 22, 204, 200, 165, 54, 138],
    })
  ) as Serializer<
    DeleteMessageInstructionDataArgs,
    DeleteMessageInstructionData
  >;
}

// Args.
export type DeleteMessageInstructionArgs = DeleteMessageInstructionDataArgs;

// Instruction.
export function deleteMessage(
  context: Pick<Context, 'programs'>,
  input: DeleteMessageInstructionAccounts & DeleteMessageInstructionArgs
): TransactionBuilder {
  // Program ID.
  const programId = context.programs.getPublicKey(
    'solchat',
    '9dBYNp2M6waWyDhpXptBAALAEuV5MHCZUUB9eTpufbbS'
  );

  // Accounts.
  const resolvedAccounts = {
    group: {
      index: 0,
      isWritable: true as boolean,
      value: input.group ?? null,
    },
    signer: {
      index: 1,
      isWritable: true as boolean,
      value: input.signer ?? null,
    },
    systemProgram: {
      index: 2,
      isWritable: false as boolean,
      value: input.systemProgram ?? null,
    },
  } satisfies ResolvedAccountsWithIndices;

  // Arguments.
  const resolvedArgs: DeleteMessageInstructionArgs = { ...input };

  // Default values.
  if (!resolvedAccounts.systemProgram.value) {
    resolvedAccounts.systemProgram.value = context.programs.getPublicKey(
      'splSystem',
      '11111111111111111111111111111111'
    );
    resolvedAccounts.systemProgram.isWritable = false;
  }

  // Accounts in order.
  const orderedAccounts: ResolvedAccount[] = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);

  // Keys and Signers.
  const [keys, signers] = getAccountMetasAndSigners(
    orderedAccounts,
    'programId',
    programId
  );

  // Data.
  const data = getDeleteMessageInstructionDataSerializer().serialize(
    resolvedArgs as DeleteMessageInstructionDataArgs
  );

  // Bytes Created On Chain.
  const bytesCreatedOnChain = 0;

  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain },
  ]);
}
