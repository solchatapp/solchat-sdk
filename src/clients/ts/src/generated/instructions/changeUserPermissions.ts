/**
 * This code was AUTOGENERATED using the kinobi library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun kinobi to update it.
 *
 * @see https://github.com/metaplex-foundation/kinobi
 */

import {
  Context,
  Pda,
  PublicKey,
  Signer,
  TransactionBuilder,
  transactionBuilder,
} from '@metaplex-foundation/umi';
import {
  Serializer,
  array,
  mapSerializer,
  publicKey as publicKeySerializer,
  struct,
  u64,
  u8,
} from '@metaplex-foundation/umi/serializers';
import {
  ResolvedAccount,
  ResolvedAccountsWithIndices,
  getAccountMetasAndSigners,
} from '../shared';
import {
  UserPermissions,
  UserPermissionsArgs,
  getUserPermissionsSerializer,
} from '../types';

// Accounts.
export type ChangeUserPermissionsInstructionAccounts = {
  group: PublicKey | Pda;
  signer: Signer;
  systemProgram?: PublicKey | Pda;
};

// Data.
export type ChangeUserPermissionsInstructionData = {
  discriminator: Array<number>;
  groupId: bigint;
  user: PublicKey;
  newPermissions: UserPermissions;
};

export type ChangeUserPermissionsInstructionDataArgs = {
  groupId: number | bigint;
  user: PublicKey;
  newPermissions: UserPermissionsArgs;
};

export function getChangeUserPermissionsInstructionDataSerializer(): Serializer<
  ChangeUserPermissionsInstructionDataArgs,
  ChangeUserPermissionsInstructionData
> {
  return mapSerializer<
    ChangeUserPermissionsInstructionDataArgs,
    any,
    ChangeUserPermissionsInstructionData
  >(
    struct<ChangeUserPermissionsInstructionData>(
      [
        ['discriminator', array(u8(), { size: 8 })],
        ['groupId', u64()],
        ['user', publicKeySerializer()],
        ['newPermissions', getUserPermissionsSerializer()],
      ],
      { description: 'ChangeUserPermissionsInstructionData' }
    ),
    (value) => ({
      ...value,
      discriminator: [244, 101, 105, 218, 194, 47, 23, 44],
    })
  ) as Serializer<
    ChangeUserPermissionsInstructionDataArgs,
    ChangeUserPermissionsInstructionData
  >;
}

// Args.
export type ChangeUserPermissionsInstructionArgs =
  ChangeUserPermissionsInstructionDataArgs;

// Instruction.
export function changeUserPermissions(
  context: Pick<Context, 'programs'>,
  input: ChangeUserPermissionsInstructionAccounts &
    ChangeUserPermissionsInstructionArgs
): TransactionBuilder {
  // Program ID.
  const programId = context.programs.getPublicKey(
    'solchat',
    '9dBYNp2M6waWyDhpXptBAALAEuV5MHCZUUB9eTpufbbS'
  );

  // Accounts.
  const resolvedAccounts = {
    group: {
      index: 0,
      isWritable: true as boolean,
      value: input.group ?? null,
    },
    signer: {
      index: 1,
      isWritable: true as boolean,
      value: input.signer ?? null,
    },
    systemProgram: {
      index: 2,
      isWritable: false as boolean,
      value: input.systemProgram ?? null,
    },
  } satisfies ResolvedAccountsWithIndices;

  // Arguments.
  const resolvedArgs: ChangeUserPermissionsInstructionArgs = { ...input };

  // Default values.
  if (!resolvedAccounts.systemProgram.value) {
    resolvedAccounts.systemProgram.value = context.programs.getPublicKey(
      'splSystem',
      '11111111111111111111111111111111'
    );
    resolvedAccounts.systemProgram.isWritable = false;
  }

  // Accounts in order.
  const orderedAccounts: ResolvedAccount[] = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);

  // Keys and Signers.
  const [keys, signers] = getAccountMetasAndSigners(
    orderedAccounts,
    'programId',
    programId
  );

  // Data.
  const data = getChangeUserPermissionsInstructionDataSerializer().serialize(
    resolvedArgs as ChangeUserPermissionsInstructionDataArgs
  );

  // Bytes Created On Chain.
  const bytesCreatedOnChain = 0;

  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain },
  ]);
}
