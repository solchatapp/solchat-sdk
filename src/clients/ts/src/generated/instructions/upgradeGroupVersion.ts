/**
 * This code was AUTOGENERATED using the kinobi library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun kinobi to update it.
 *
 * @see https://github.com/metaplex-foundation/kinobi
 */

import {
  Context,
  Pda,
  PublicKey,
  Signer,
  TransactionBuilder,
  transactionBuilder,
} from '@metaplex-foundation/umi';
import {
  Serializer,
  array,
  mapSerializer,
  struct,
  u64,
  u8,
} from '@metaplex-foundation/umi/serializers';
import {
  ResolvedAccount,
  ResolvedAccountsWithIndices,
  getAccountMetasAndSigners,
} from '../shared';

// Accounts.
export type UpgradeGroupVersionInstructionAccounts = {
  group: PublicKey | Pda;
  owner: Signer;
  systemProgram?: PublicKey | Pda;
};

// Data.
export type UpgradeGroupVersionInstructionData = {
  discriminator: Array<number>;
  groupId: bigint;
  encryptedUserKeys: Array<Array<number>>;
};

export type UpgradeGroupVersionInstructionDataArgs = {
  groupId: number | bigint;
  encryptedUserKeys: Array<Array<number>>;
};

export function getUpgradeGroupVersionInstructionDataSerializer(): Serializer<
  UpgradeGroupVersionInstructionDataArgs,
  UpgradeGroupVersionInstructionData
> {
  return mapSerializer<
    UpgradeGroupVersionInstructionDataArgs,
    any,
    UpgradeGroupVersionInstructionData
  >(
    struct<UpgradeGroupVersionInstructionData>(
      [
        ['discriminator', array(u8(), { size: 8 })],
        ['groupId', u64()],
        ['encryptedUserKeys', array(array(u8(), { size: 128 }))],
      ],
      { description: 'UpgradeGroupVersionInstructionData' }
    ),
    (value) => ({
      ...value,
      discriminator: [236, 243, 181, 70, 239, 221, 197, 150],
    })
  ) as Serializer<
    UpgradeGroupVersionInstructionDataArgs,
    UpgradeGroupVersionInstructionData
  >;
}

// Args.
export type UpgradeGroupVersionInstructionArgs =
  UpgradeGroupVersionInstructionDataArgs;

// Instruction.
export function upgradeGroupVersion(
  context: Pick<Context, 'programs'>,
  input: UpgradeGroupVersionInstructionAccounts &
    UpgradeGroupVersionInstructionArgs
): TransactionBuilder {
  // Program ID.
  const programId = context.programs.getPublicKey(
    'solchat',
    '9dBYNp2M6waWyDhpXptBAALAEuV5MHCZUUB9eTpufbbS'
  );

  // Accounts.
  const resolvedAccounts = {
    group: {
      index: 0,
      isWritable: true as boolean,
      value: input.group ?? null,
    },
    owner: {
      index: 1,
      isWritable: true as boolean,
      value: input.owner ?? null,
    },
    systemProgram: {
      index: 2,
      isWritable: false as boolean,
      value: input.systemProgram ?? null,
    },
  } satisfies ResolvedAccountsWithIndices;

  // Arguments.
  const resolvedArgs: UpgradeGroupVersionInstructionArgs = { ...input };

  // Default values.
  if (!resolvedAccounts.systemProgram.value) {
    resolvedAccounts.systemProgram.value = context.programs.getPublicKey(
      'splSystem',
      '11111111111111111111111111111111'
    );
    resolvedAccounts.systemProgram.isWritable = false;
  }

  // Accounts in order.
  const orderedAccounts: ResolvedAccount[] = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);

  // Keys and Signers.
  const [keys, signers] = getAccountMetasAndSigners(
    orderedAccounts,
    'programId',
    programId
  );

  // Data.
  const data = getUpgradeGroupVersionInstructionDataSerializer().serialize(
    resolvedArgs as UpgradeGroupVersionInstructionDataArgs
  );

  // Bytes Created On Chain.
  const bytesCreatedOnChain = 0;

  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain },
  ]);
}
