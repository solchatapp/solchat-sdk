/**
 * This code was AUTOGENERATED using the kinobi library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun kinobi to update it.
 *
 * @see https://github.com/metaplex-foundation/kinobi
 */

import {
  Context,
  Pda,
  PublicKey,
  Signer,
  TransactionBuilder,
  transactionBuilder,
} from '@metaplex-foundation/umi';
import {
  Serializer,
  array,
  mapSerializer,
  string,
  struct,
  u64,
  u8,
} from '@metaplex-foundation/umi/serializers';
import {
  ResolvedAccount,
  ResolvedAccountsWithIndices,
  getAccountMetasAndSigners,
} from '../shared';

// Accounts.
export type ChangeGroupNameInstructionAccounts = {
  group: PublicKey | Pda;
  signer: Signer;
  systemProgram?: PublicKey | Pda;
};

// Data.
export type ChangeGroupNameInstructionData = {
  discriminator: Array<number>;
  groupId: bigint;
  newName: string;
};

export type ChangeGroupNameInstructionDataArgs = {
  groupId: number | bigint;
  newName: string;
};

export function getChangeGroupNameInstructionDataSerializer(): Serializer<
  ChangeGroupNameInstructionDataArgs,
  ChangeGroupNameInstructionData
> {
  return mapSerializer<
    ChangeGroupNameInstructionDataArgs,
    any,
    ChangeGroupNameInstructionData
  >(
    struct<ChangeGroupNameInstructionData>(
      [
        ['discriminator', array(u8(), { size: 8 })],
        ['groupId', u64()],
        ['newName', string()],
      ],
      { description: 'ChangeGroupNameInstructionData' }
    ),
    (value) => ({
      ...value,
      discriminator: [23, 34, 155, 13, 219, 127, 115, 35],
    })
  ) as Serializer<
    ChangeGroupNameInstructionDataArgs,
    ChangeGroupNameInstructionData
  >;
}

// Args.
export type ChangeGroupNameInstructionArgs = ChangeGroupNameInstructionDataArgs;

// Instruction.
export function changeGroupName(
  context: Pick<Context, 'programs'>,
  input: ChangeGroupNameInstructionAccounts & ChangeGroupNameInstructionArgs
): TransactionBuilder {
  // Program ID.
  const programId = context.programs.getPublicKey(
    'solchat',
    '9dBYNp2M6waWyDhpXptBAALAEuV5MHCZUUB9eTpufbbS'
  );

  // Accounts.
  const resolvedAccounts = {
    group: {
      index: 0,
      isWritable: true as boolean,
      value: input.group ?? null,
    },
    signer: {
      index: 1,
      isWritable: true as boolean,
      value: input.signer ?? null,
    },
    systemProgram: {
      index: 2,
      isWritable: false as boolean,
      value: input.systemProgram ?? null,
    },
  } satisfies ResolvedAccountsWithIndices;

  // Arguments.
  const resolvedArgs: ChangeGroupNameInstructionArgs = { ...input };

  // Default values.
  if (!resolvedAccounts.systemProgram.value) {
    resolvedAccounts.systemProgram.value = context.programs.getPublicKey(
      'splSystem',
      '11111111111111111111111111111111'
    );
    resolvedAccounts.systemProgram.isWritable = false;
  }

  // Accounts in order.
  const orderedAccounts: ResolvedAccount[] = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);

  // Keys and Signers.
  const [keys, signers] = getAccountMetasAndSigners(
    orderedAccounts,
    'programId',
    programId
  );

  // Data.
  const data = getChangeGroupNameInstructionDataSerializer().serialize(
    resolvedArgs as ChangeGroupNameInstructionDataArgs
  );

  // Bytes Created On Chain.
  const bytesCreatedOnChain = 0;

  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain },
  ]);
}
