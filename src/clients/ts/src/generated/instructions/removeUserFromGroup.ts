/**
 * This code was AUTOGENERATED using the kinobi library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun kinobi to update it.
 *
 * @see https://github.com/metaplex-foundation/kinobi
 */

import {
  Context,
  Pda,
  PublicKey,
  Signer,
  TransactionBuilder,
  transactionBuilder,
} from '@metaplex-foundation/umi';
import {
  Serializer,
  array,
  mapSerializer,
  publicKey as publicKeySerializer,
  struct,
  u64,
  u8,
} from '@metaplex-foundation/umi/serializers';
import {
  ResolvedAccount,
  ResolvedAccountsWithIndices,
  getAccountMetasAndSigners,
} from '../shared';

// Accounts.
export type RemoveUserFromGroupInstructionAccounts = {
  group: PublicKey | Pda;
  userAcc: PublicKey | Pda;
  signer: Signer;
  systemProgram?: PublicKey | Pda;
};

// Data.
export type RemoveUserFromGroupInstructionData = {
  discriminator: Array<number>;
  groupId: bigint;
  user: PublicKey;
};

export type RemoveUserFromGroupInstructionDataArgs = {
  groupId: number | bigint;
  user: PublicKey;
};

export function getRemoveUserFromGroupInstructionDataSerializer(): Serializer<
  RemoveUserFromGroupInstructionDataArgs,
  RemoveUserFromGroupInstructionData
> {
  return mapSerializer<
    RemoveUserFromGroupInstructionDataArgs,
    any,
    RemoveUserFromGroupInstructionData
  >(
    struct<RemoveUserFromGroupInstructionData>(
      [
        ['discriminator', array(u8(), { size: 8 })],
        ['groupId', u64()],
        ['user', publicKeySerializer()],
      ],
      { description: 'RemoveUserFromGroupInstructionData' }
    ),
    (value) => ({
      ...value,
      discriminator: [129, 244, 192, 29, 81, 105, 218, 33],
    })
  ) as Serializer<
    RemoveUserFromGroupInstructionDataArgs,
    RemoveUserFromGroupInstructionData
  >;
}

// Args.
export type RemoveUserFromGroupInstructionArgs =
  RemoveUserFromGroupInstructionDataArgs;

// Instruction.
export function removeUserFromGroup(
  context: Pick<Context, 'programs'>,
  input: RemoveUserFromGroupInstructionAccounts &
    RemoveUserFromGroupInstructionArgs
): TransactionBuilder {
  // Program ID.
  const programId = context.programs.getPublicKey(
    'solchat',
    '9dBYNp2M6waWyDhpXptBAALAEuV5MHCZUUB9eTpufbbS'
  );

  // Accounts.
  const resolvedAccounts = {
    group: {
      index: 0,
      isWritable: true as boolean,
      value: input.group ?? null,
    },
    userAcc: {
      index: 1,
      isWritable: true as boolean,
      value: input.userAcc ?? null,
    },
    signer: {
      index: 2,
      isWritable: true as boolean,
      value: input.signer ?? null,
    },
    systemProgram: {
      index: 3,
      isWritable: false as boolean,
      value: input.systemProgram ?? null,
    },
  } satisfies ResolvedAccountsWithIndices;

  // Arguments.
  const resolvedArgs: RemoveUserFromGroupInstructionArgs = { ...input };

  // Default values.
  if (!resolvedAccounts.systemProgram.value) {
    resolvedAccounts.systemProgram.value = context.programs.getPublicKey(
      'splSystem',
      '11111111111111111111111111111111'
    );
    resolvedAccounts.systemProgram.isWritable = false;
  }

  // Accounts in order.
  const orderedAccounts: ResolvedAccount[] = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);

  // Keys and Signers.
  const [keys, signers] = getAccountMetasAndSigners(
    orderedAccounts,
    'programId',
    programId
  );

  // Data.
  const data = getRemoveUserFromGroupInstructionDataSerializer().serialize(
    resolvedArgs as RemoveUserFromGroupInstructionDataArgs
  );

  // Bytes Created On Chain.
  const bytesCreatedOnChain = 0;

  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain },
  ]);
}
