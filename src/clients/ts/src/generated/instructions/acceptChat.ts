/**
 * This code was AUTOGENERATED using the kinobi library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun kinobi to update it.
 *
 * @see https://github.com/metaplex-foundation/kinobi
 */

import {
  Context,
  Pda,
  PublicKey,
  Signer,
  TransactionBuilder,
  transactionBuilder,
} from '@metaplex-foundation/umi';
import {
  Serializer,
  array,
  mapSerializer,
  publicKey as publicKeySerializer,
  struct,
  u8,
} from '@metaplex-foundation/umi/serializers';
import {
  ResolvedAccount,
  ResolvedAccountsWithIndices,
  getAccountMetasAndSigners,
} from '../shared';

// Accounts.
export type AcceptChatInstructionAccounts = {
  userAcc: PublicKey | Pda;
  signer: Signer;
  systemProgram?: PublicKey | Pda;
};

// Data.
export type AcceptChatInstructionData = {
  discriminator: Array<number>;
  group: PublicKey;
};

export type AcceptChatInstructionDataArgs = { group: PublicKey };

export function getAcceptChatInstructionDataSerializer(): Serializer<
  AcceptChatInstructionDataArgs,
  AcceptChatInstructionData
> {
  return mapSerializer<
    AcceptChatInstructionDataArgs,
    any,
    AcceptChatInstructionData
  >(
    struct<AcceptChatInstructionData>(
      [
        ['discriminator', array(u8(), { size: 8 })],
        ['group', publicKeySerializer()],
      ],
      { description: 'AcceptChatInstructionData' }
    ),
    (value) => ({
      ...value,
      discriminator: [222, 173, 134, 246, 161, 240, 119, 96],
    })
  ) as Serializer<AcceptChatInstructionDataArgs, AcceptChatInstructionData>;
}

// Args.
export type AcceptChatInstructionArgs = AcceptChatInstructionDataArgs;

// Instruction.
export function acceptChat(
  context: Pick<Context, 'programs'>,
  input: AcceptChatInstructionAccounts & AcceptChatInstructionArgs
): TransactionBuilder {
  // Program ID.
  const programId = context.programs.getPublicKey(
    'solchat',
    '9dBYNp2M6waWyDhpXptBAALAEuV5MHCZUUB9eTpufbbS'
  );

  // Accounts.
  const resolvedAccounts = {
    userAcc: {
      index: 0,
      isWritable: true as boolean,
      value: input.userAcc ?? null,
    },
    signer: {
      index: 1,
      isWritable: true as boolean,
      value: input.signer ?? null,
    },
    systemProgram: {
      index: 2,
      isWritable: false as boolean,
      value: input.systemProgram ?? null,
    },
  } satisfies ResolvedAccountsWithIndices;

  // Arguments.
  const resolvedArgs: AcceptChatInstructionArgs = { ...input };

  // Default values.
  if (!resolvedAccounts.systemProgram.value) {
    resolvedAccounts.systemProgram.value = context.programs.getPublicKey(
      'splSystem',
      '11111111111111111111111111111111'
    );
    resolvedAccounts.systemProgram.isWritable = false;
  }

  // Accounts in order.
  const orderedAccounts: ResolvedAccount[] = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);

  // Keys and Signers.
  const [keys, signers] = getAccountMetasAndSigners(
    orderedAccounts,
    'programId',
    programId
  );

  // Data.
  const data = getAcceptChatInstructionDataSerializer().serialize(
    resolvedArgs as AcceptChatInstructionDataArgs
  );

  // Bytes Created On Chain.
  const bytesCreatedOnChain = 0;

  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain },
  ]);
}
