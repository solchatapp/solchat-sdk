/**
 * This code was AUTOGENERATED using the kinobi library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun kinobi to update it.
 *
 * @see https://github.com/metaplex-foundation/kinobi
 */

import {
  Context,
  Pda,
  PublicKey,
  Signer,
  TransactionBuilder,
  transactionBuilder,
} from '@metaplex-foundation/umi';
import {
  Serializer,
  array,
  mapSerializer,
  publicKey as publicKeySerializer,
  struct,
  u64,
  u8,
} from '@metaplex-foundation/umi/serializers';
import {
  ResolvedAccount,
  ResolvedAccountsWithIndices,
  getAccountMetasAndSigners,
} from '../shared';

// Accounts.
export type AddUserToGroupInstructionAccounts = {
  group: PublicKey | Pda;
  userAcc: PublicKey | Pda;
  signer: Signer;
  systemProgram?: PublicKey | Pda;
};

// Data.
export type AddUserToGroupInstructionData = {
  discriminator: Array<number>;
  groupId: bigint;
  user: PublicKey;
  encryptedUserKey: Array<number>;
};

export type AddUserToGroupInstructionDataArgs = {
  groupId: number | bigint;
  user: PublicKey;
  encryptedUserKey: Array<number>;
};

export function getAddUserToGroupInstructionDataSerializer(): Serializer<
  AddUserToGroupInstructionDataArgs,
  AddUserToGroupInstructionData
> {
  return mapSerializer<
    AddUserToGroupInstructionDataArgs,
    any,
    AddUserToGroupInstructionData
  >(
    struct<AddUserToGroupInstructionData>(
      [
        ['discriminator', array(u8(), { size: 8 })],
        ['groupId', u64()],
        ['user', publicKeySerializer()],
        ['encryptedUserKey', array(u8(), { size: 128 })],
      ],
      { description: 'AddUserToGroupInstructionData' }
    ),
    (value) => ({
      ...value,
      discriminator: [26, 89, 243, 119, 58, 242, 166, 51],
    })
  ) as Serializer<
    AddUserToGroupInstructionDataArgs,
    AddUserToGroupInstructionData
  >;
}

// Args.
export type AddUserToGroupInstructionArgs = AddUserToGroupInstructionDataArgs;

// Instruction.
export function addUserToGroup(
  context: Pick<Context, 'programs'>,
  input: AddUserToGroupInstructionAccounts & AddUserToGroupInstructionArgs
): TransactionBuilder {
  // Program ID.
  const programId = context.programs.getPublicKey(
    'solchat',
    '9dBYNp2M6waWyDhpXptBAALAEuV5MHCZUUB9eTpufbbS'
  );

  // Accounts.
  const resolvedAccounts = {
    group: {
      index: 0,
      isWritable: true as boolean,
      value: input.group ?? null,
    },
    userAcc: {
      index: 1,
      isWritable: true as boolean,
      value: input.userAcc ?? null,
    },
    signer: {
      index: 2,
      isWritable: true as boolean,
      value: input.signer ?? null,
    },
    systemProgram: {
      index: 3,
      isWritable: false as boolean,
      value: input.systemProgram ?? null,
    },
  } satisfies ResolvedAccountsWithIndices;

  // Arguments.
  const resolvedArgs: AddUserToGroupInstructionArgs = { ...input };

  // Default values.
  if (!resolvedAccounts.systemProgram.value) {
    resolvedAccounts.systemProgram.value = context.programs.getPublicKey(
      'splSystem',
      '11111111111111111111111111111111'
    );
    resolvedAccounts.systemProgram.isWritable = false;
  }

  // Accounts in order.
  const orderedAccounts: ResolvedAccount[] = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);

  // Keys and Signers.
  const [keys, signers] = getAccountMetasAndSigners(
    orderedAccounts,
    'programId',
    programId
  );

  // Data.
  const data = getAddUserToGroupInstructionDataSerializer().serialize(
    resolvedArgs as AddUserToGroupInstructionDataArgs
  );

  // Bytes Created On Chain.
  const bytesCreatedOnChain = 0;

  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain },
  ]);
}
