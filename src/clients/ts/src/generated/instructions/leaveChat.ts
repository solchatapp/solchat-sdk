/**
 * This code was AUTOGENERATED using the kinobi library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun kinobi to update it.
 *
 * @see https://github.com/metaplex-foundation/kinobi
 */

import {
  Context,
  Pda,
  PublicKey,
  Signer,
  TransactionBuilder,
  transactionBuilder,
} from '@metaplex-foundation/umi';
import {
  Serializer,
  array,
  mapSerializer,
  publicKey as publicKeySerializer,
  struct,
  u8,
} from '@metaplex-foundation/umi/serializers';
import {
  ResolvedAccount,
  ResolvedAccountsWithIndices,
  getAccountMetasAndSigners,
} from '../shared';

// Accounts.
export type LeaveChatInstructionAccounts = {
  userAcc: PublicKey | Pda;
  signer: Signer;
  systemProgram?: PublicKey | Pda;
};

// Data.
export type LeaveChatInstructionData = {
  discriminator: Array<number>;
  group: PublicKey;
};

export type LeaveChatInstructionDataArgs = { group: PublicKey };

export function getLeaveChatInstructionDataSerializer(): Serializer<
  LeaveChatInstructionDataArgs,
  LeaveChatInstructionData
> {
  return mapSerializer<
    LeaveChatInstructionDataArgs,
    any,
    LeaveChatInstructionData
  >(
    struct<LeaveChatInstructionData>(
      [
        ['discriminator', array(u8(), { size: 8 })],
        ['group', publicKeySerializer()],
      ],
      { description: 'LeaveChatInstructionData' }
    ),
    (value) => ({
      ...value,
      discriminator: [33, 33, 11, 135, 89, 243, 113, 102],
    })
  ) as Serializer<LeaveChatInstructionDataArgs, LeaveChatInstructionData>;
}

// Args.
export type LeaveChatInstructionArgs = LeaveChatInstructionDataArgs;

// Instruction.
export function leaveChat(
  context: Pick<Context, 'programs'>,
  input: LeaveChatInstructionAccounts & LeaveChatInstructionArgs
): TransactionBuilder {
  // Program ID.
  const programId = context.programs.getPublicKey(
    'solchat',
    '9dBYNp2M6waWyDhpXptBAALAEuV5MHCZUUB9eTpufbbS'
  );

  // Accounts.
  const resolvedAccounts = {
    userAcc: {
      index: 0,
      isWritable: true as boolean,
      value: input.userAcc ?? null,
    },
    signer: {
      index: 1,
      isWritable: true as boolean,
      value: input.signer ?? null,
    },
    systemProgram: {
      index: 2,
      isWritable: false as boolean,
      value: input.systemProgram ?? null,
    },
  } satisfies ResolvedAccountsWithIndices;

  // Arguments.
  const resolvedArgs: LeaveChatInstructionArgs = { ...input };

  // Default values.
  if (!resolvedAccounts.systemProgram.value) {
    resolvedAccounts.systemProgram.value = context.programs.getPublicKey(
      'splSystem',
      '11111111111111111111111111111111'
    );
    resolvedAccounts.systemProgram.isWritable = false;
  }

  // Accounts in order.
  const orderedAccounts: ResolvedAccount[] = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);

  // Keys and Signers.
  const [keys, signers] = getAccountMetasAndSigners(
    orderedAccounts,
    'programId',
    programId
  );

  // Data.
  const data = getLeaveChatInstructionDataSerializer().serialize(
    resolvedArgs as LeaveChatInstructionDataArgs
  );

  // Bytes Created On Chain.
  const bytesCreatedOnChain = 0;

  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain },
  ]);
}
